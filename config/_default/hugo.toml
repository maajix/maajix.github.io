# -- Site Configuration --
# Refer to the theme docs for more details about each of these parameters.
# https://blowfish.page/docs/getting-started/

theme = "blowfish" # UNCOMMENT THIS LINE
# baseURL = "https://your_domain.com/"
defaultContentLanguage = "en"

# pluralizeListTitles = "true" # hugo function useful for non-english languages, find out more in  https://gohugo.io/getting-started/configuration/#pluralizelisttitles

enableRobotsTXT = true

# Sitemap lastmod should be the last real change to a page, which is what Google
# and Bing actually read. Without this it is the front matter date, so an edited
# page keeps advertising its original publication date. The Pages workflow
# checks out with fetch-depth: 0, so the history is there to read.
enableGitInfo = true
summaryLength = 0

buildDrafts = false
buildFuture = false

enableEmoji = true

# googleAnalytics = "G-XXXXXXXXX"

# Zen mode button only on blog posts, not CTFs/resume/other pages
[[cascade]]
  showZenMode = true
  [cascade.target]
    path = "/posts/**"

# Keep taxonomy pages out of the sitemap. 45 of the 70 entries were tag pages
# and 34 of those list a single post, so they are duplicates of that post's
# title and summary with no content of their own. They stay crawlable and
# linked from every article; they are just not advertised as important URLs.
[[cascade]]
  [cascade.sitemap]
    disable = true
  [cascade.target]
    kind = "{taxonomy,term}"

[pagination]
  pagerSize = 100

[imaging]
  anchor = 'Center'

[taxonomies]
  tag = "tags"
  category = "categories"
  author = "authors"
  series = "series"

[sitemap]
  # No changefreq or priority: Google ignores both outright, Bing ignores them
  # too, and a constant value on every URL carries no information anyway.
  # https://developers.google.com/search/docs/crawling-indexing/sitemaps/build-sitemap
  filename = 'sitemap.xml'

# Plain-text site index for LLM crawlers (Blowfish ships layouts/_default/llms.txt).
# Note: as of 2026 no major AI vendor has committed to reading /llms.txt, and
# server-log studies show it is rarely fetched. Kept because it costs nothing.
[outputFormats.llms]
  mediaType = "text/plain"
  baseName = "llms"
  isPlainText = true
  rel = "alternate"

[outputs]
  home = ["HTML", "RSS", "JSON", "LLMS"]

[related]
  threshold = 0
  toLower = false

    [[related.indices]]
        name = "tags"
        weight = 100

    [[related.indices]]
        name = "categories"
        weight = 100

    [[related.indices]]
        name = "series"
        weight = 50

    [[related.indices]]
        name = "authors"
        weight = 20

    [[related.indices]]
        name = "date"
        weight = 10

    [[related.indices]]
      applyFilter = false
      name = 'fragmentrefs'
      type = 'fragments'
      weight = 10
